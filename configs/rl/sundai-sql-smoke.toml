# Short hosted RL smoke: same env as sundai-sql.toml, tiny step budget.
model = "Qwen/Qwen3-4B-Instruct-2507"
max_steps = 10
batch_size = 64
rollouts_per_example = 8

[sampling]
max_tokens = 1024

[[env]]
id = "atiyil/sundai-rlvr-sqlExecutor"
